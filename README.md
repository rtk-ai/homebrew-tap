# homebrew-tap

Homebrew tap for [rtk-ai](https://github.com/rtk-ai) tools.

## Install

```bash
brew tap rtk-ai/tap
```

## Available formulas

| Formula | Description |
|---------|-------------|
| `rtk` | High-performance CLI proxy to minimize LLM token consumption |
| `vox` | TTS CLI for macOS — local voice synthesis with Qwen and system say |
| `icm` | Permanent memory for AI agents |

```bash
brew install rtk
brew install vox
brew install icm
```
